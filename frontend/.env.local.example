# Paste the ApiUrl output from: cdk deploy Todos2Stack-dev
# Used by Vite's dev server proxy so local requests to /todos are forwarded to AWS.
# Not needed for production builds (VITE_API_URL is used instead at build time).
DEV_API_URL=https://xxxx.execute-api.us-east-1.amazonaws.com
